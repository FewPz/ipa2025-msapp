apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: rabbit-auth
  namespace: ipa2025
spec:
  secretTargetRef:
    - parameter: connection
      name: rabbitmq-conn
      key: RABBITMQ_URI