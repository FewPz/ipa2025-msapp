apiVersion: apps/v1
kind: Deployment
metadata:
  name: ipa2025-msapp-scheduler
  namespace: ipa2025
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ipa2025-msapp-scheduler
  template:
    metadata:
      labels:
        app: ipa2025-msapp-scheduler
    spec:
      containers:
        - name: scheduler
          image: fewza49144/scheduler:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: s-5d2lw
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
      restartPolicy: Always